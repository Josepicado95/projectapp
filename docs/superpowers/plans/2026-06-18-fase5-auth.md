# Fase 5 — Authentication Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar login, registro y protección de rutas con Auth.js v5, y reemplazar `userId: 1` hardcodeado en todas las Server Actions por el id real del usuario autenticado.

**Architecture:** Auth.js v5 con estrategia JWT y proveedor Credentials (email + contraseña). Las contraseñas se hashean con bcryptjs antes de guardar. El middleware de Next.js protege todas las rutas privadas. Los Server Components leen la sesión directamente con `auth()`. No se necesita SessionProvider porque no se usa `useSession()` en Client Components.

**Tech Stack:** Next.js 16 (App Router), Auth.js v5 (`next-auth@beta`), bcryptjs, Prisma 7, zod, TypeScript.

## Global Constraints

- `next-auth@beta` — Auth.js v5, NO instalar `next-auth` sin el tag `@beta`.
- `bcryptjs` (no `bcrypt`) — no requiere compilación nativa, funciona en Windows sin problemas.
- `AUTH_SECRET` debe existir en `.env` antes de que cualquier cosa de Auth.js funcione.
- `"use server"` en la primera línea de archivos de acciones; `"use client"` en la primera línea de componentes de cliente.
- `userId: 1` debe desaparecer completamente — reemplazar por `Number(session.user.id)`.
- Nombres de variables y funciones en inglés; UI y mensajes de error en español.
- Sin tests automatizados — verificación manual en el navegador (tests son Fase 11).
- El campo `password` ya existe en el modelo `User` del schema de Prisma — no se necesita migración.

---

## File Map

| Acción | Archivo |
|---|---|
| Crear | `apps/web/auth.ts` |
| Crear | `apps/web/middleware.ts` |
| Crear | `apps/web/app/api/auth/[...nextauth]/route.ts` |
| Crear | `apps/web/app/actions/auth.ts` |
| Crear | `apps/web/app/register/page.tsx` |
| Crear | `apps/web/app/login/page.tsx` |
| Modificar | `apps/web/prisma/seed.ts` |
| Modificar | `apps/web/app/actions/adventures.ts` |
| Modificar | `apps/web/app/actions/missions.ts` |
| Modificar | `apps/web/app/page.tsx` |

---

## Task 1: Instalar dependencias + generar AUTH_SECRET + configurar Auth.js

**Files:**
- Create: `apps/web/auth.ts`
- Create: `apps/web/app/api/auth/[...nextauth]/route.ts`
- Modify: `apps/web/.env` (agregar AUTH_SECRET)

**Interfaces:**
- Produces: `auth()` — función que devuelve la sesión actual (usada en Server Components y Server Actions)
- Produces: `handlers` — GET y POST handlers para las rutas de Auth.js
- Produces: `signIn`, `signOut` — funciones para iniciar/cerrar sesión

- [ ] **Step 1: Instalar dependencias**

```bash
cd apps/web && npm install next-auth@beta bcryptjs && npm install --save-dev @types/bcryptjs
```

Esperado: `next-auth@beta` y `bcryptjs` en `package.json`. Si hay warning sobre `peer dependencies`, es normal.

- [ ] **Step 2: Generar AUTH_SECRET**

```bash
cd apps/web && npx auth secret
```

Esto agrega automáticamente `AUTH_SECRET=<valor-generado>` al archivo `.env`. Si el comando falla (requiere git), alternativa manual:

```bash
# En PowerShell:
[Convert]::ToBase64String([Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
# Copiar el resultado y agregar manualmente a .env:
# AUTH_SECRET=<resultado>
```

Verificar que `.env` contiene la línea `AUTH_SECRET=...` antes de continuar.

- [ ] **Step 3: Crear `apps/web/auth.ts`**

```typescript
import NextAuth, { DefaultSession } from "next-auth";
import Credentials from "next-auth/providers/credentials";
import bcrypt from "bcryptjs";
import { prisma } from "@/lib/prisma";

declare module "next-auth" {
  interface Session {
    user: { id: string } & DefaultSession["user"];
  }
}

export const { handlers, auth, signIn, signOut } = NextAuth({
  providers: [
    Credentials({
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Contraseña", type: "password" },
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) return null;

        const user = await prisma.user.findUnique({
          where: { email: String(credentials.email) },
        });

        if (!user) return null;

        const passwordMatch = await bcrypt.compare(
          String(credentials.password),
          user.password
        );

        if (!passwordMatch) return null;

        return { id: String(user.id), name: user.name, email: user.email };
      },
    }),
  ],
  session: { strategy: "jwt" },
  callbacks: {
    jwt({ token, user }) {
      if (user) token.id = user.id;
      return token;
    },
    session({ session, token }) {
      if (token.id) session.user.id = token.id as string;
      return session;
    },
  },
  pages: {
    signIn: "/login",
  },
});
```

- [ ] **Step 4: Crear `apps/web/app/api/auth/[...nextauth]/route.ts`**

Crear el directorio y el archivo:

```typescript
import { handlers } from "@/auth";
export const { GET, POST } = handlers;
```

Este archivo es el "receptor" de todas las peticiones que Auth.js necesita manejar internamente (ej: `/api/auth/session`, `/api/auth/callback/credentials`).

- [ ] **Step 5: Verificar TypeScript**

```bash
cd apps/web && npx tsc --noEmit
```

Esperado: sin errores. Si hay error sobre `bcryptjs`, verificar que `@types/bcryptjs` está instalado.

- [ ] **Step 6: Commit**

```bash
git add apps/web/auth.ts "apps/web/app/api/auth/[...nextauth]/route.ts" apps/web/package.json apps/web/package-lock.json apps/web/.env
git commit -m "feat: add Auth.js v5 config with credentials provider"
```

---

## Task 2: Actualizar seed con contraseña hasheada + re-seedear la DB

**Files:**
- Modify: `apps/web/prisma/seed.ts`

**Interfaces:**
- Produces: usuario `jose@aventuras.com` con contraseña `aventuras123` (hasheada con bcrypt) en la DB

- [ ] **Step 1: Modificar `apps/web/prisma/seed.ts`**

Agregar el import de bcryptjs al inicio del archivo, y reemplazar `password: "placeholder-hasta-fase-5"` por un hash real.

Reemplazar el archivo completo con:

```typescript
import "dotenv/config";
import { PrismaClient } from "../lib/generated/prisma/client";
import { PrismaPg } from "@prisma/adapter-pg";
import bcrypt from "bcryptjs";

const prisma = new PrismaClient({
  adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL }),
});

async function main() {
  await prisma.checkIn.deleteMany();
  await prisma.mission.deleteMany();
  await prisma.adventure.deleteMany();
  await prisma.user.deleteMany();

  const hashedPassword = await bcrypt.hash("aventuras123", 12);

  const user = await prisma.user.create({
    data: {
      email: "jose@aventuras.com",
      name: "Jose",
      password: hashedPassword,
    },
  });

  const adventure1 = await prisma.adventure.create({
    data: {
      title: "Aprender programación",
      description: "Construir una app full-stack desde cero",
      status: "active",
      userId: user.id,
      missions: {
        create: [
          { title: "Completar Fase 1 del roadmap", difficulty: 1, completed: true },
          { title: "Completar Fase 2 del roadmap", difficulty: 1, completed: true },
          { title: "Montar PostgreSQL con Docker", difficulty: 2, completed: false },
          { title: "Escribir el schema de Prisma", difficulty: 2, completed: false },
          { title: "Conectar el frontend a la DB real", difficulty: 3, completed: false },
        ],
      },
    },
  });

  const adventure2 = await prisma.adventure.create({
    data: {
      title: "Mejorar condición física",
      description: "Correr 5km sin parar en 3 meses",
      status: "active",
      userId: user.id,
      missions: {
        create: [
          { title: "Correr 1km sin parar", difficulty: 1, completed: false },
          { title: "Correr 3km sin parar", difficulty: 2, completed: false },
          { title: "Correr 5km sin parar", difficulty: 3, completed: false },
        ],
      },
    },
  });

  await prisma.checkIn.createMany({
    data: [
      { userId: user.id, energy: 4, mood: 5, stress: 2, sleep: 8 },
      { userId: user.id, energy: 3, mood: 3, stress: 3, sleep: 6 },
      { userId: user.id, energy: 5, mood: 4, stress: 1, sleep: 7 },
    ],
  });

  console.log("Seed completado:");
  console.log(`  Usuario: ${user.email} / contraseña: aventuras123`);
  console.log(`  Aventura 1: ${adventure1.title}`);
  console.log(`  Aventura 2: ${adventure2.title}`);
  console.log("  3 check-ins creados");
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

- [ ] **Step 2: Re-ejecutar el seed**

```bash
cd apps/web && npx prisma db seed
```

Esperado: "Seed completado: Usuario: jose@aventuras.com / contraseña: aventuras123"

- [ ] **Step 3: Commit**

```bash
git add apps/web/prisma/seed.ts
git commit -m "feat: update seed with bcrypt-hashed password for test user"
```

---

## Task 3: Server Action de registro + página de registro

**Files:**
- Create: `apps/web/app/actions/auth.ts`
- Create: `apps/web/app/register/page.tsx`

**Interfaces:**
- Consumes: `prisma.user.findUnique`, `prisma.user.create`
- Consumes: `bcrypt.hash`
- Consumes: `redirect` de `next/navigation`
- Produces: `registerAction(prevState, formData)` → `Promise<RegisterState>`
- Produces: `loginAction(prevState, formData)` → `Promise<LoginState>` (se completa en Task 4)
- Produces: `logoutAction()` → `Promise<void>`
- Tipo `RegisterState` = `{ errors?: { name?: string[]; email?: string[]; password?: string[] }; general?: string }`
- Tipo `LoginState` = `{ error?: string }`

- [ ] **Step 1: Crear `apps/web/app/actions/auth.ts`**

```typescript
"use server";

import { z } from "zod";
import { redirect } from "next/navigation";
import { AuthError } from "next-auth";
import bcrypt from "bcryptjs";
import { prisma } from "@/lib/prisma";
import { signIn, signOut } from "@/auth";

type RegisterState = {
  errors?: { name?: string[]; email?: string[]; password?: string[] };
  general?: string;
};

type LoginState = { error?: string };

const RegisterSchema = z.object({
  name: z.string().min(2, "El nombre debe tener al menos 2 caracteres"),
  email: z.string().email("Email inválido"),
  password: z.string().min(8, "La contraseña debe tener al menos 8 caracteres"),
});

export async function registerAction(
  prevState: RegisterState,
  formData: FormData
): Promise<RegisterState> {
  const result = RegisterSchema.safeParse({
    name: formData.get("name"),
    email: formData.get("email"),
    password: formData.get("password"),
  });

  if (!result.success) {
    return { errors: result.error.flatten().fieldErrors };
  }

  const existing = await prisma.user.findUnique({
    where: { email: result.data.email },
  });

  if (existing) {
    return { errors: { email: ["Ya existe una cuenta con ese email"] } };
  }

  const hashedPassword = await bcrypt.hash(result.data.password, 12);

  await prisma.user.create({
    data: {
      name: result.data.name,
      email: result.data.email,
      password: hashedPassword,
    },
  });

  redirect("/login");
}

export async function loginAction(
  prevState: LoginState,
  formData: FormData
): Promise<LoginState> {
  try {
    await signIn("credentials", {
      email: formData.get("email"),
      password: formData.get("password"),
      redirectTo: "/",
    });
  } catch (error) {
    if (error instanceof AuthError) {
      return { error: "Email o contraseña incorrectos" };
    }
    throw error;
  }
  return {};
}

export async function logoutAction(): Promise<void> {
  await signOut({ redirectTo: "/login" });
}
```

- [ ] **Step 2: Crear `apps/web/app/register/page.tsx`**

```typescript
"use client";

import { useActionState } from "react";
import Link from "next/link";
import { registerAction } from "@/app/actions/auth";

const initialState = {};

export default function RegisterPage() {
  const [state, formAction, pending] = useActionState(registerAction, initialState);

  return (
    <main className="max-w-sm mx-auto p-8">
      <h1 className="text-2xl font-bold mb-6">Crear cuenta</h1>

      <form action={formAction} className="space-y-4">
        <div>
          <label className="block text-sm font-medium mb-1">Nombre</label>
          <input
            name="name"
            type="text"
            required
            className="border rounded px-3 py-2 w-full"
          />
          {state.errors?.name && (
            <p className="text-red-500 text-sm mt-1">{state.errors.name[0]}</p>
          )}
        </div>

        <div>
          <label className="block text-sm font-medium mb-1">Email</label>
          <input
            name="email"
            type="email"
            required
            className="border rounded px-3 py-2 w-full"
          />
          {state.errors?.email && (
            <p className="text-red-500 text-sm mt-1">{state.errors.email[0]}</p>
          )}
        </div>

        <div>
          <label className="block text-sm font-medium mb-1">Contraseña</label>
          <input
            name="password"
            type="password"
            required
            className="border rounded px-3 py-2 w-full"
          />
          {state.errors?.password && (
            <p className="text-red-500 text-sm mt-1">{state.errors.password[0]}</p>
          )}
        </div>

        {state.general && (
          <p className="text-red-500 text-sm">{state.general}</p>
        )}

        <button
          type="submit"
          disabled={pending}
          className="bg-indigo-500 text-white px-4 py-2 rounded w-full disabled:opacity-50"
        >
          {pending ? "Creando cuenta..." : "Crear cuenta"}
        </button>
      </form>

      <p className="text-sm text-center mt-4">
        ¿Ya tienes cuenta?{" "}
        <Link href="/login" className="text-indigo-500">
          Inicia sesión
        </Link>
      </p>
    </main>
  );
}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
cd apps/web && npx tsc --noEmit
```

Esperado: sin errores.

- [ ] **Step 4: Verificar manualmente**

Con `npm run dev`, abrir `http://localhost:3000/register`:
- Intentar registrar con contraseña de 5 caracteres → error de validación
- Intentar registrar con email ya existente (`jose@aventuras.com`) → error "Ya existe una cuenta"
- Registrar con datos válidos → redirige a `/login`
- Verificar en Prisma Studio que el nuevo usuario aparece con password hasheada (empieza con `$2b$...`)

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/actions/auth.ts apps/web/app/register/page.tsx
git commit -m "feat: add register server action and register page"
```

---

## Task 4: Página de login

**Files:**
- Create: `apps/web/app/login/page.tsx`

**Interfaces:**
- Consumes: `loginAction` de `@/app/actions/auth`
- Consumes: `useActionState` de `react`
- Produces: formulario de login en `/login` que redirige a `/` al autenticarse

- [ ] **Step 1: Crear `apps/web/app/login/page.tsx`**

```typescript
"use client";

import { useActionState } from "react";
import Link from "next/link";
import { loginAction } from "@/app/actions/auth";

const initialState = {};

export default function LoginPage() {
  const [state, formAction, pending] = useActionState(loginAction, initialState);

  return (
    <main className="max-w-sm mx-auto p-8">
      <h1 className="text-2xl font-bold mb-6">Iniciar sesión</h1>

      <form action={formAction} className="space-y-4">
        <div>
          <label className="block text-sm font-medium mb-1">Email</label>
          <input
            name="email"
            type="email"
            required
            className="border rounded px-3 py-2 w-full"
          />
        </div>

        <div>
          <label className="block text-sm font-medium mb-1">Contraseña</label>
          <input
            name="password"
            type="password"
            required
            className="border rounded px-3 py-2 w-full"
          />
        </div>

        {state.error && (
          <p className="text-red-500 text-sm">{state.error}</p>
        )}

        <button
          type="submit"
          disabled={pending}
          className="bg-indigo-500 text-white px-4 py-2 rounded w-full disabled:opacity-50"
        >
          {pending ? "Entrando..." : "Entrar"}
        </button>
      </form>

      <p className="text-sm text-center mt-4">
        ¿No tienes cuenta?{" "}
        <Link href="/register" className="text-indigo-500">
          Crear cuenta
        </Link>
      </p>
    </main>
  );
}
```

- [ ] **Step 2: Verificar manualmente**

Con `npm run dev`, abrir `http://localhost:3000/login`:
- Intentar con contraseña incorrecta → mensaje "Email o contraseña incorrectos"
- Iniciar sesión con `jose@aventuras.com` / `aventuras123` → redirige a `/`
- Verificar que la sesión persiste al recargar la página (el Dashboard carga sin pedir login de nuevo)

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/login/page.tsx
git commit -m "feat: add login page"
```

---

## Task 5: Middleware de protección de rutas

**Files:**
- Create: `apps/web/middleware.ts`

**Interfaces:**
- Consumes: `auth` de `@/auth` (re-exportado como `middleware`)
- Produces: redirección automática a `/login` para rutas no autenticadas
- Produces: redirección a `/` si usuario autenticado intenta acceder a `/login` o `/register`

Nota: el archivo `middleware.ts` debe estar en la raíz de `apps/web/` (mismo nivel que `app/`, no dentro de él).

- [ ] **Step 1: Crear `apps/web/middleware.ts`**

```typescript
import { auth } from "@/auth";
import { NextResponse } from "next/server";

export default auth((req) => {
  const isLoggedIn = !!req.auth;
  const isAuthPage = req.nextUrl.pathname.startsWith("/login") ||
    req.nextUrl.pathname.startsWith("/register");

  if (isAuthPage && isLoggedIn) {
    return NextResponse.redirect(new URL("/", req.url));
  }

  if (!isAuthPage && !isLoggedIn) {
    return NextResponse.redirect(new URL("/login", req.url));
  }
});

export const config = {
  matcher: ["/((?!api/auth|_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 2: Verificar manualmente**

Con `npm run dev`:
- Cerrar sesión (borrar cookies manualmente en DevTools → Application → Cookies → borrar `authjs.session-token`) y recargar `/` → debe redirigir a `/login`
- Iniciar sesión → redirige a `/`
- Estando autenticado, intentar ir a `/login` → debe redirigir a `/`

- [ ] **Step 3: Commit**

```bash
git add apps/web/middleware.ts
git commit -m "feat: add middleware for route protection"
```

---

## Task 6: Reemplazar `userId: 1` — Server Actions y Dashboard

**Files:**
- Modify: `apps/web/app/actions/adventures.ts`
- Modify: `apps/web/app/actions/missions.ts`
- Modify: `apps/web/app/page.tsx`

**Interfaces:**
- Consumes: `auth()` de `@/auth` en todas las Server Actions y en `app/page.tsx`
- Produces: todas las operaciones CRUD usan el userId real de la sesión
- Produces: Dashboard solo muestra las aventuras del usuario autenticado

- [ ] **Step 1: Reemplazar `userId` en `apps/web/app/actions/adventures.ts`**

Agregar el import de `auth` al inicio del archivo (después del `"use server"`):

```typescript
import { auth } from "@/auth";
```

Reemplazar la función `createAdventure` completa:

```typescript
export async function createAdventure(
  prevState: ActionState,
  formData: FormData
): Promise<ActionState> {
  const session = await auth();
  if (!session?.user?.id) throw new Error("No autorizado");
  const userId = Number(session.user.id);

  const result = CreateAdventureSchema.safeParse({
    title: formData.get("title"),
    description: formData.get("description") || undefined,
  });

  if (!result.success) {
    return { errors: result.error.flatten().fieldErrors };
  }

  await prisma.adventure.create({
    data: {
      title: result.data.title,
      description: result.data.description,
      userId,
    },
  });

  revalidatePath("/");
  return { message: "¡Aventura creada!" };
}
```

- [ ] **Step 2: Reemplazar `userId` en `updateAdventure` de `adventures.ts`**

`updateAdventure` y `deleteAdventure` no crean nuevas aventuras, pero deben verificar que la aventura pertenece al usuario antes de modificarla. Reemplazar ambas funciones:

```typescript
export async function updateAdventure(formData: FormData): Promise<void> {
  const session = await auth();
  if (!session?.user?.id) throw new Error("No autorizado");

  const result = UpdateAdventureSchema.safeParse({
    id: formData.get("id"),
    title: formData.get("title"),
    description: formData.get("description") || undefined,
    status: formData.get("status"),
  });

  if (!result.success) {
    throw new Error("Datos inválidos al actualizar la aventura");
  }

  await prisma.adventure.update({
    where: { id: result.data.id, userId: Number(session.user.id) },
    data: {
      title: result.data.title,
      description: result.data.description,
      status: result.data.status,
    },
  });

  revalidatePath("/");
}

export async function deleteAdventure(formData: FormData): Promise<void> {
  const session = await auth();
  if (!session?.user?.id) throw new Error("No autorizado");

  const id = Number(formData.get("id"));
  if (!id) return;

  await prisma.adventure.delete({
    where: { id, userId: Number(session.user.id) },
  });

  revalidatePath("/");
}
```

Nota: `where: { id, userId }` en Prisma garantiza que solo se puede eliminar/actualizar una aventura que pertenezca a ese usuario — aunque alguien manipule el id en el form.

- [ ] **Step 3: Reemplazar `userId` en `apps/web/app/actions/missions.ts`**

Agregar el import de `auth` al inicio del archivo (después del `"use server"`):

```typescript
import { auth } from "@/auth";
```

Las acciones de misiones no usan `userId` directamente (las misiones pertenecen a aventuras), pero deben verificar autenticación. Agregar al inicio de cada función:

```typescript
const session = await auth();
if (!session?.user?.id) throw new Error("No autorizado");
```

Reemplazar el archivo completo con:

```typescript
"use server";

import { z } from "zod";
import { revalidatePath } from "next/cache";
import { prisma } from "@/lib/prisma";
import { auth } from "@/auth";

type ActionState = {
  errors?: { title?: string[]; difficulty?: string[] };
  message?: string;
};

const CreateMissionSchema = z.object({
  adventureId: z.coerce.number().int().positive(),
  title: z.string().min(3, "El título debe tener al menos 3 caracteres"),
  description: z.string().optional(),
  difficulty: z.coerce.number().int().min(1).max(3),
});

const UpdateMissionSchema = z.object({
  id: z.coerce.number().int().positive(),
  adventureId: z.coerce.number().int().positive(),
  title: z.string().min(3, "El título debe tener al menos 3 caracteres"),
  description: z.string().optional(),
  difficulty: z.coerce.number().int().min(1).max(3),
});

export async function createMission(
  prevState: ActionState,
  formData: FormData
): Promise<ActionState> {
  const session = await auth();
  if (!session?.user?.id) throw new Error("No autorizado");

  const result = CreateMissionSchema.safeParse({
    adventureId: formData.get("adventureId"),
    title: formData.get("title"),
    description: formData.get("description") || undefined,
    difficulty: formData.get("difficulty"),
  });

  if (!result.success) {
    return { errors: result.error.flatten().fieldErrors };
  }

  await prisma.mission.create({
    data: {
      title: result.data.title,
      description: result.data.description,
      difficulty: result.data.difficulty,
      adventureId: result.data.adventureId,
    },
  });

  revalidatePath(`/adventures/${result.data.adventureId}`);
  return { message: "¡Misión creada!" };
}

export async function updateMission(formData: FormData): Promise<void> {
  const session = await auth();
  if (!session?.user?.id) return;

  const result = UpdateMissionSchema.safeParse({
    id: formData.get("id"),
    adventureId: formData.get("adventureId"),
    title: formData.get("title"),
    description: formData.get("description") || undefined,
    difficulty: formData.get("difficulty"),
  });

  if (!result.success) return;

  await prisma.mission.update({
    where: { id: result.data.id },
    data: {
      title: result.data.title,
      description: result.data.description,
      difficulty: result.data.difficulty,
    },
  });

  revalidatePath(`/adventures/${result.data.adventureId}`);
}

export async function toggleMission(formData: FormData): Promise<void> {
  const session = await auth();
  if (!session?.user?.id) return;

  const id = Number(formData.get("id"));
  const adventureId = Number(formData.get("adventureId"));
  if (!id || !adventureId) return;

  const mission = await prisma.mission.findUnique({ where: { id } });
  if (!mission) return;

  await prisma.mission.update({
    where: { id },
    data: {
      completed: !mission.completed,
      completedAt: !mission.completed ? new Date() : null,
    },
  });

  revalidatePath(`/adventures/${adventureId}`);
}

export async function deleteMission(formData: FormData): Promise<void> {
  const session = await auth();
  if (!session?.user?.id) return;

  const id = Number(formData.get("id"));
  const adventureId = Number(formData.get("adventureId"));
  if (!id || !adventureId) return;

  await prisma.mission.delete({ where: { id } });
  revalidatePath(`/adventures/${adventureId}`);
}
```

- [ ] **Step 4: Actualizar `apps/web/app/page.tsx`**

Reemplazar el archivo completo:

```typescript
import { redirect } from "next/navigation";
import { auth } from "@/auth";
import AdventureCard from "@/components/AdventureCard";
import NewAdventureForm from "@/components/NewAdventureForm";
import { prisma } from "@/lib/prisma";
import { logoutAction } from "@/app/actions/auth";

export default async function Home() {
  const session = await auth();
  if (!session?.user) redirect("/login");

  const userId = Number(session.user.id);

  const adventures = await prisma.adventure.findMany({
    where: { userId },
    include: { missions: true },
    orderBy: { createdAt: "desc" },
  });

  return (
    <main className="max-w-2xl mx-auto p-4">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">Mis Aventuras</h1>
        <div className="flex items-center gap-3">
          <span className="text-sm text-gray-500">Hola, {session.user.name}</span>
          <form action={logoutAction}>
            <button type="submit" className="text-sm border px-3 py-1 rounded">
              Cerrar sesión
            </button>
          </form>
        </div>
      </div>

      <NewAdventureForm />

      {adventures.length === 0 ? (
        <p className="text-gray-400 text-center py-8">No tienes aventuras todavía.</p>
      ) : (
        adventures.map((adventure) => (
          <AdventureCard key={adventure.id} adventure={adventure} />
        ))
      )}
    </main>
  );
}
```

- [ ] **Step 5: Verificar TypeScript**

```bash
cd apps/web && npx tsc --noEmit
```

Esperado: sin errores.

- [ ] **Step 6: Verificar el flujo completo**

Con `npm run dev`:

1. Ir a `/` sin sesión → redirige a `/login`
2. Login con `jose@aventuras.com` / `aventuras123` → Dashboard con "Hola, Jose" y botón "Cerrar sesión"
3. Verificar que el Dashboard solo muestra las aventuras de Jose
4. Crear una aventura nueva → aparece en el Dashboard
5. Ir a `/adventures/[id]` → crear, completar y eliminar misiones
6. Cerrar sesión → redirige a `/login`
7. Registrar una nueva cuenta → redirige a `/login`
8. Login con la cuenta nueva → Dashboard vacío (sin aventuras, porque es un usuario diferente)

- [ ] **Step 7: Commit**

```bash
git add apps/web/app/actions/adventures.ts apps/web/app/actions/missions.ts apps/web/app/page.tsx
git commit -m "feat: replace hardcoded userId with real session user in all actions"
```

---

## Checklist de cierre de Fase 5

- [ ] Login con credenciales correctas → redirige al Dashboard
- [ ] Login con credenciales incorrectas → mensaje de error inline
- [ ] Registro con datos válidos → redirige a `/login`
- [ ] Registro con email duplicado → error "Ya existe una cuenta"
- [ ] Ruta `/` sin sesión → redirige a `/login`
- [ ] Ruta `/login` con sesión activa → redirige a `/`
- [ ] Dashboard muestra "Hola, [nombre]" del usuario autenticado
- [ ] Dashboard filtra aventuras por usuario (cuenta nueva = dashboard vacío)
- [ ] Cerrar sesión → redirige a `/login` y borra la sesión
- [ ] `npx tsc --noEmit` sin errores
- [ ] Actualizar ROADMAP.md marcando los checkboxes de Fase 5 como completados
